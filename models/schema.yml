version: 2
models:
  - name: dim_category
    columns:
    - name: category_id
      tests:
      - not_null
      - unique

  - name: dim_city
    columns:
      - name: city_id
        tests:
           - not_null
           - unique
       

  - name: dim_customer
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique
          
      
  

    